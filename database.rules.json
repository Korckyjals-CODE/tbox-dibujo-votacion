{
  // Dormant lockdown (app unused until possibly next year): deny-by-default.
  // Do NOT reopen public .write on /meta or /votes without reviewing SETUP.md —
  // restore voting rules carefully when the certamen is revived.
  "rules": {
    ".read": false,
    ".write": false
  }
}
